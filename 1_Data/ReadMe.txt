1_Data -> Convert the file names of the raw data downloaded from the data center to file names that are interpretable by the different procedures

Set the parameters in the dedicated section:

%% Parameters
% path to the raw data downloaded from the data center
folder_in = 'path_folder_in/' ;

% path to the output folder to store the renamed files
folder_out = 'path_folder_out/' ;